version: "3"
services:
  cmd:
    container_name: arbitrage-htc
    build:
      context: .
      dockerfile: Dockerfile
    # restart: always
    env_file: .env
    volumes:
      - ./config:/config
      - ./logs:/logs
      - ./imgs:/imgs
