version: "3"
services:
  cmd:
    container_name: binance-mexc-cmd
    build:
      context: .
      dockerfile: Dockerfile
    # restart: always
    volumes:
      - ./config.yaml:/config.yaml
      - ./logs:/logs
